/** Width of a chord-row bar (chord row no longer needs to align with the piano roll). */
export const BAR_W = 220
/** Sticky-left title column width. */
export const LEFT_COL_W = 128
/** "+" add-bar button width. */
export const ADD_BAR_W = 32

/** Piano-roll cell dimensions (single-measure view, generous size). */
export const PR_CELL_W = 16
export const PR_CELL_H = 11
/** Piano-roll sticky pitch-label column width. */
export const PR_KEY_W = 56
